1.What is JSX, and why is it used?
Ans:JSX(Javascript XML) is a syntax extension for Javascript that allows writing HTML -like code inside Javascript.It used to makes code easier to read and write.



2.What is the difference between State and Props?
Ans:State is used for managing component data.Props are used to pass data between components.



3.What is the useState hook, and how does it work?
Ans:The useState hook is used to manage state in functional components.




4.How can you share state between components in React?
Ans:There are several ways: lift state up- move state to the closest common parent component. Props drilling: pass state through props.





5.How is event handling done in React?
Ans:Event handling in React is similar to JavaScript but uses camelCase naming.Use JSX event attributes like onClick, onChange.Pass function reference, not function call.